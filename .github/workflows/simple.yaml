name: Shell Commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hey DevOps fans!"
  reusable-test:
    runs-on: ubuntu-latest
    uses: ./.github/workflows/reusable
    with:
      caller_name: "Bond"
