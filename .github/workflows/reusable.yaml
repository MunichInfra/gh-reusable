name: Reusable workflow example

on:
  workflow_call:
    inputs:
      caller_name:
        required: true
        type: string

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: Echo from Reusable
        run: echo "Hello Mr. {{ inputs.caller_name }}"
